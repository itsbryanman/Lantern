services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped
    command:
      - --database
      - /database/filebrowser.db
      - --root
      - ${FILEBROWSER_ROOT_PATH}
      - --port
      - "80"
    volumes:
      - ../data/filebrowser/database:/database
      - ${FILEBROWSER_ROOT_PATH}:${FILEBROWSER_ROOT_PATH}
    networks:
      - lantern
    labels:
      - traefik.enable=true
      - traefik.docker.network=lantern
      - traefik.http.routers.filebrowser.rule=Host(`files.${LANTERN_DOMAIN}`)
      - traefik.http.routers.filebrowser.entrypoints=websecure
      - traefik.http.routers.filebrowser.tls.certresolver=letsencrypt
      - traefik.http.routers.filebrowser.middlewares=default-chain@file
      - traefik.http.services.filebrowser.loadbalancer.server.port=80

networks:
  lantern:
    name: lantern
